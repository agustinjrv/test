{
  "name": "Task Board",
  "install": "npm install",
  "terminals": [
    {
      "name": "api",
      "command": "npm run dev:api",
      "description": "Express + TypeScript API server on port 3001 (auto-reloads via tsx watch)."
    },
    {
      "name": "web",
      "command": "npm run dev:web",
      "description": "Vite + React dev server on port 5173, proxies /api to the API server."
    }
  ],
  "ports": [
    { "name": "web", "port": 5173 },
    { "name": "api", "port": 3001 }
  ]
}
